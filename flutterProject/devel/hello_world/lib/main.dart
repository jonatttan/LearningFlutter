import 'package:flutter/material.dart';

void main() {
  // runApp(const MyApp());
  runApp(
    MaterialApp(
      home: Center(
        child: Text(
          'Hello world!',
          style: TextStyle(fontSize: 30, color: Colors.blue),
        ),
      ),
    ),
  );
}
